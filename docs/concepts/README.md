# Core Concepts

- **[Handlers](handlers.md)** — event, call, block, and file handlers; the `#[handler]` macro.
- **[Entities](entities.md)** — `#[derive(Entity)]`, the builder API, loading and saving.
- **[Primitives](primitives.md)** — `BigInt`, `BigDecimal`, `Bytes`, and `Address`.
- **[Testing](testing.md)** — native `cargo test` with the mock store.
- **[Event Context Reference](event-context.md)** — all fields on `EventContext`, `CallContext`, and `FileContext`.
